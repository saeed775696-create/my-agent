import { logger } from "../core/logger.js";

// Placeholder for future Google Sheets integration
export async function appendToSheet(data: any) {
  logger.info("Google Sheets sync disabled", data);
}
